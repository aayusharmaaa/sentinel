numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
fastapi>=0.104
uvicorn>=0.24
pydantic>=2.0
# Optional: only needed for --live mode (real VLM / narrative calls).
# The full pipeline and the entire evaluation run offline without it.
# anthropic>=0.40
